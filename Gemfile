# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

# TODO: Release `filewatcher` and change it
# gem 'filewatcher', github: 'filewatcher/filewatcher', branch: 'add_plugin_system'
gem 'filewatcher', path: '~/Projects/ruby/filewatcher'

gem 'filewatcher-spinner', path: '~/Projects/ruby/filewatcher-spinner'
